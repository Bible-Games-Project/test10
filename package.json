{
  "name": "test10",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@capacitor/core": "^8.4.2",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/ios": "^8.4.2",
    "@capacitor/android": "^8.4.2"
  }
}